<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.jmarque.cpsc399project.MainActivity">

    <EditText
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/mainActivity_username"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="15dp"
        android:hint="Username" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Log In"
        android:onClick="loginClick"
        android:id="@+id/mainActivity_loginButton"
        android:background="@color/colorPrimary"
        android:textColor="#fff"
        android:layout_below="@+id/mainActivity_username"
        android:layout_alignEnd="@+id/mainActivity_username" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Register"
        android:onClick="registerClick"
        android:id="@+id/mainActivity_registerButton"
        android:background="@color/colorAccent"
        android:textColor="#fff"
        android:layout_alignTop="@+id/mainActivity_loginButton"
        android:layout_alignStart="@+id/mainActivity_username" />
</RelativeLayout>
